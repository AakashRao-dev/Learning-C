#include <stdio.h>

int main() {
  int marks[] = {12, 42, 34, 54, 21};

  // printf("Enter the marks for student 1: ");
  // scanf(" %d", &marks[0]);

  // printf("Enter the marks for student 2: ");
  // scanf(" %d", &marks[1]);

  // printf("Enter the marks for student 3: ");
  // scanf(" %d", &marks[2]);

  // printf("Enter the marks for student 4: ");
  // scanf(" %d", &marks[3]);

  // printf("Enter the marks for student 5: ");
  // scanf(" %d", &marks[4]);

  printf("\nMarks for Student 1 is: %d", marks[0]);
  printf("\nMarks for Student 2 is: %d", marks[1]);
  printf("\nMarks for Student 3 is: %d", marks[2]);
  printf("\nMarks for Student 4 is: %d", marks[3]);
  printf("\nMarks for Student 5 is: %d", marks[4]);

  return 0;
}